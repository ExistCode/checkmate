"use client";

import React from "react";

// Placeholder provider for future context/state if needed.
export default function CognitoProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

